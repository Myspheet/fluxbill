# API base URL the SPA calls. Default uses the Vite dev proxy (/api -> :8000).
# Set to an absolute URL (e.g. https://api.fluxbill.app/api) in production.
VITE_API_URL=/api

# Backend origin the dev proxy forwards /api to.
VITE_BACKEND_URL=http://localhost:8000
